interface:
  display_name: "Init Deep"
  short_description: "Write a required root AGENTS.md and optional explicitly requested child files"
  default_prompt: "Use $init-deep to inspect this repository, write the root AGENTS.md, and add child AGENTS.md files only for directories the user explicitly names."
